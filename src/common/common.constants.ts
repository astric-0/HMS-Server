export const QUEUE_NAMES = Object.freeze({
  MEDIA_QUEUE: 'media-queue',
  FILE_EXTRACT_QUEUE: 'file-extract-queue',
});

export const JOBS_NAMES = Object.freeze({
  DOWNLOAD_MEDIA: 'download-media',
  EXTRACT_FILE: 'extract-file',
});
